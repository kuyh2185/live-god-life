# 다음에 얘기할 수정 사항

## 1. 혼나기 모드(방해꾼) 켤 때 권한 요청을 간단하게
- 원하는 것: 혼나기 모드를 켜면 "알림을 허용하시겠습니까?" 창처럼 버튼만 누르면 되는 권한 창이 두 번 연달아 떴으면 좋겠음.
- 필요한 권한 두 개(안드로이드 기준, 짐작):
  1. 사용 기록 접근: 유튜브·인스타 등을 얼마나 썼는지 알아야 해서
  2. 다른 앱 위에 표시: 딴짓 중인 앱 위에 고양이 화면을 띄우려면 필요
- 확인할 점: 이 두 권한은 알림 권한과 달리 안드로이드가 "허용" 팝업을 제공하지 않고, 설정 화면으로 보내야만 켤 수 있음.
  → 현실적인 방법: 앱 안에서 "허용" 창을 두 번 띄우고, 누를 때마다 해당 설정 화면으로 바로 이동 → 켜고 돌아오면 다음 창.
- 지금 프로토타입에는 아직 이 권한 흐름이 없음(버튼으로 흉내만 냄).

## 2. "오늘 기록 공유하기" 버튼 이름 바꾸기
- 위치: 히스토리 탭의 공유 버튼, 공유 창 제목.
- 원하는 방향: "오늘 비서와의 활동 공유" 같은 느낌으로, 대신 짧게.
- 후보: "오늘 활동 공유", "비서랑 한 하루 공유" 등(같이 정하기).

## 3. 미리보기 버튼 하나로 합치기
- 원하는 것: "딴짓 감지"와 "일정 관리" 쪽에 미리보기 버튼이 따로 있는데, 둘 다 같은 미리보기 화면을 보여줄 거라면 버튼(화면)은 하나만 있으면 됨.
- 확인할 점: 음성 메모라 정확히 어느 버튼들인지 다시 확인 필요.
  (지금 코드 기준으로는 방해꾼 탭 규칙마다 있는 "▶ 딴짓해보기" 버튼일 가능성)

## 4. 고양이 배경 지우고, 고양이는 하나로 고정
- 지금 문제: 고양이 사진에 방 배경이 그대로 보임.
- 원하는 것: 고양이를 잘라내는 게 아니라 배경을 지워서(누끼) 고양이만 남기기.
- 고양이는 고르게 하지 말고 한 마리로 고정해서 넣기 → 방해꾼 탭의 "고양이 고르기" 5종 선택은 빼는 방향.
- 같이 정할 것: 어떤 고양이로 고정할지, 배경 지운 뒤 펀치 화면 뒤에 무엇을 깔지(앱 화면 그대로 보이게 할지).

## 5. 비서 말투 프리셋
- 원하는 것: 비서 말투를 여러 프리셋 중에서 고를 수 있게.
- 기본값: 정중한 존댓말. 일반적인 비서처럼, 서비스직 같은 마인드로 친절하고 깔끔하게.
- 다른 프리셋 후보(같이 정하기): 친구처럼 반말, 고양이 말투("~냥"), 단호한 코치 등.
- 참고: 지금 방해꾼 대사는 전부 "~냥" 말투로 고정돼 있음 → 프리셋에 맞춰 바뀌게 할지도 정하기.

## 6. 혼나기(방해꾼) 실제 작동 확인 → 원인 확인됨
- 증상: 유튜브에 들어가도 아무것도 안 뜸.
- 원인: 고장이 아니라 아직 안 만든 기능. 지금 앱은 다른 앱을 쓰는지 감지하는 코드가 전혀 없고,
  방해꾼 탭의 "▶ 딴짓해보기" 버튼으로만 흉내 내는 단계(1단계).
- 참고: 규칙 조건도 "하루 30분 넘게 쓰면"이라, 감지가 생겨도 바로 뜨진 않음.
- 실제로 작동하게 하려면(2단계): 사용 기록 접근 권한 + 다른 앱 위에 표시 권한 + 백그라운드에서 사용 시간을 확인하는 안드로이드 네이티브 코드 필요(1번 권한 흐름과 같이 진행).

## 7. 음성 비서: 이어폰으로 읽어주기 + 양방향 대화 → 원인 확인됨
- 목표: 최종적으로 "양방향 음성 비서" 일정 관리 앱. 이어폰으로 알림을 읽어주고, 말로 대답도 할 수 있게.
- 증상: 이어폰으로 음성이 오지 않음.
- 코드 확인 결과 안 되는 이유 3가지:
  1. 이어폰 연결을 실제로 감지하지 않음. 설정의 "이어폰 연결 시뮬레이션" 스위치를 직접 켜야 읽어주는 구조이고, 기본값은 꺼짐.
  2. 읽어주기는 브라우저 음성 기능(speechSynthesis)을 쓰는데, 안드로이드 앱 안(WebView)에서는 대부분 작동하지 않음.
     → 앱에서 쓰려면 안드로이드 음성(TTS) 플러그인이 필요.
  3. 말로 대답하는 기능(음성 인식)은 아예 없음. 지금 대화는 채팅 입력창으로만 가능.
- 해야 할 것: 실제 이어폰 감지 + 네이티브 TTS 플러그인 + 음성 인식 플러그인 + 채팅(AI 서버)과 연결.

## 8. 기상·취침 알림이 안 옴 + 기능 자체가 이해하기 어려움 → 원인 확인됨
- 증상: 눌러봐도 오는 게 없고, 몇 시에 뭐가 오는 건지 알기 어려움.
- 원인:
  - 앱에 실제 알림 플러그인(@capacitor/local-notifications)이 아직 설치돼 있지 않음 → 휴대폰 알림이 하나도 안 옴(일정 알림 포함).
  - 기상·취침 시간은 저장만 되고, 그 시간에 알림을 예약하는 코드가 없음.
  - "지금 미리보기"는 기상 = 컨디션 체크 창을 띄우고, 취침 = 화면 아래에 내일 일정 한 줄을 잠깐 보여주는 것뿐.
- 해야 할 것:
  1. 알림 플러그인 설치 후 기상·취침 시간에 실제 알림 예약(NOTIFICATIONS.md 참고).
  2. 설정 화면에 "매일 오전 7시에 이런 알림이 와요"처럼 무엇이 언제 오는지 예시로 보여주기.

## 9. 못 한 일정 "오늘 다시 잡기"는 지금 시간 기준으로
- 증상: 오후 3시 30분 일정을 못 해서 "오늘 다시 잡기"를 누르면, 시간이 원래 시간(3:30)에서 시작함.
  이미 지난 시간이라 이상함.
- 원인(코드 확인): 다시 잡기 화면이 원래 일정 시간을 그대로 기본값으로 씀. 지난 시간으로 내려가는 것도 막혀 있지 않음.
- 제안:
  1. 기본값을 "지금 이후 비어 있는 시간"으로 맞추기(빈 시간 찾는 기능이 이미 있음). 없으면 지금 시각을 5분 단위로 올려서.
  2. 오늘 일정은 지금보다 이전 시간으로는 못 내리게 막기.
- 같이 볼 것: "조금 했어요/얼마나 했는지"를 고르면 "다음 계획에 참고할게요"라고 나오지만, 실제로는 히스토리에
  표시만 되고 다음 일정 제안에는 아직 반영되지 않음 → 다음 일정에 어떻게 피드백을 줄지 정하기
  (예: 자주 "시작만 했어요"면 다음엔 시간을 줄이거나 가볍게 버전을 먼저 제안).

## 10. 비서 성향(조용히 / 적당히 / 적극적으로) 차이가 눈에 보이게
- 증상: 단계를 바꿔도 달라지는 게 안 보임.
- 원인(코드 확인): 지금 차이는 오늘 탭의 "겹침·일정 조정 제안 카드"가 언제 뜨는지뿐.
  - 조용히: 카드 안 뜸 / 적당히: 컨디션 "안 좋음"일 때만 / 적극적으로: 일정이 깨어 있는 시간보다 많을 때도.
  - 이 조건들이 자주 안 생겨서 사실상 차이를 느끼기 어려움.
- 원칙(결정됨): 화면 잠금처럼 강제로 막는 기능은 넣지 않음. 모든 단계에서 사용자가 거절할 수 있어야 함.
- 제안(단계별로 눈에 보이는 차이):
  | | 조용히 | 적당히(기본) | 적극적으로 |
  |---|---|---|---|
  | 먼저 말 걸기 | 안 함, 물어볼 때만 | 컨디션 체크 후 제안 | 바빠 보이면 먼저 제안 |
  | 알림 | 일정 시작 알림만 | + 끝난 일정 결과 확인 | + "다음 일정까지 N분", 하루 마무리 리뷰 |
  | 방해꾼 | 쳐다보기까지만 | 정한 강도대로 | 방해꾼 켜기를 먼저 권함(켤지는 사용자가) |
  | 비서 말풍선 | 거의 없음 | 가끔 | 자주, 응원·코칭 |
- 바꿀 때 "이제 이렇게 달라져요" 미리보기를 보여주고, 오늘 탭에 지금 단계를 작게 표시하기.
- 같이 정할 것:
  - "적극적으로"를 유료 플랜 기능으로 묶을지.
  - "강제 없음" 원칙과 방해꾼 3단계 "진짜 잠금"이 부딪힘 → 잠금 단계를 빼거나 이름·동작을 바꿀지.

## 대기 중
- 고양이 영상(Gemini 완성본): 다운로드해서 아티팩트나 `assets/kittens/`에 올려주면 앱에 붙이기.
