{
  "name": "godsaeng-app",
  "version": "0.1.0",
  "private": true,
  "description": "갓생살자 — Capacitor로 감싼 개인 일정 비서 앱(비서 LLM 채팅 포함)",
  "scripts": {
    "build": "node scripts/build.js",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
